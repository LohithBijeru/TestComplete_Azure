name: Build1

stages:
- stage: QA
  displayName: QA
  jobs:
  - job: Job1
    displayName: Job1
    pool:
      vmImage: 'windows-2022'   # 🔥 Microsoft-hosted Windows Server 2022 agent

    steps:
    - checkout: self

    - task: InstallTestCompleteAdapter@1
      displayName: "Install TestComplete Adapter"

    - task: VSTest@2
      displayName: "Run TestComplete Project Suite"
      inputs:
        testSelector: 'testAssemblies'
        testAssemblyVer2: |
          **\*.pjs
        searchFolder: '$(System.DefaultWorkingDirectory)'
        vsTestVersion: 'toolsInstaller'
        testRunTitle: 'TestComplete – Windows 2022 Support Check'